% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_plots.R
\name{plot_bivariate}
\alias{plot_bivariate}
\title{Bivariate scatter with SSz, Fisher, and cellMCD contours}
\usage{
plot_bivariate(
  obj,
  vars,
  qnt = 0.99,
  xlim = c(-5, 5),
  ylim = c(-5, 5),
  pch = 19,
  col = "darkgray",
  cex = 1.05,
  ...
)
}
\arguments{
\item{obj}{A cwMCD result. The full list returned by \code{cwMCD()} with
elements \code{$cellmcd.obj} and \code{$res.tbl}.}

\item{vars}{Character vector of length 2 giving the two \strong{input} column names.
You may also pass integer column
indices (referring to the \code{input.*} columns inside \code{res.tbl}).}

\item{qnt}{Numeric in (0,1). Inclusion level for contours (default \code{0.99}).}

\item{xlim, ylim}{Numeric length-2 vectors for plot limits (default \code{c(-5,5)}).}

\item{pch, col, cex}{Base plot point style (passed to \code{points()}).}

\item{...}{Additional arguments passed to \code{plot()}.}
}
\value{
(Invisibly) a list with the x/y vectors used to draw the three contours.
}
\description{
Draws a bivariate scatterplot for a chosen pair of variables from a cwMCD result
and overlays: the SSz circular boundary, Fisher’s method boundary, and the
cellMCD ellipse at the given inclusion level (default 0.99).
}
\details{
SSz boundary is the circle of radius \eqn{\sqrt{qchisq(qnt, 2)}}.
Fisher boundary is the level set of \eqn{-2\sum\log(p_i)} at the same quantile
(converted back to z via \eqn{z = \Phi^{-1}(p/2)}), drawn symmetrically in all quadrants.
The cellMCD boundary is an ellipse based on the robust covariance \code{S}
returned by \code{cellWise::cellMCD}, drawn with \code{ellipse::ellipse}.
}
\examples{
\dontrun{
set.seed(123)
X = matrix(rnorm(1000*5), nrow = 1000, ncol = 5)
mcd = cwMCD(X)
plot_bivariate(mcd, vars = c(1,2), qnt = 0.99)
}

}
