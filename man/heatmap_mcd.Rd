% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_plots.R
\name{heatmap_mcd}
\alias{heatmap_mcd}
\title{Heatmap of top rows under selected methods (panel F style)}
\usage{
heatmap_mcd(
  obj,
  methods = c("MCD", "Fisher", "SSz"),
  top_n = 25,
  palette = "Green-Orange",
  show_rownames = FALSE,
  col_labels = NULL
)
}
\arguments{
\item{obj}{A cwMCD result list or its \code{res.tbl}.}

\item{methods}{Character vector among \code{c("MCD","Fisher","SSz")}.
Order controls the left-to-right arrangement (default: all three).}

\item{top_n}{Integer; number of top rows per method (default \code{25}).}

\item{palette}{Character; name of \code{hcl.colors()} palette
(default \code{"Green-Orange"}).}

\item{show_rownames}{Logical; whether to draw row names next to each block (default \code{FALSE}).}

\item{col_labels}{Optional character vector to draw under each block (default \code{NULL}).}
}
\value{
Invisibly returns a list with the matrices used for each method block.
}
\description{
Draws adjacent heatmaps of the top \code{top_n} rows according to the chosen
method(s), using the \code{input.*} columns from a cwMCD result. Rows within each
block are ordered by the projection score used in the manuscript.
}
\examples{
\dontrun{

#' set.seed(123)
X = matrix(rnorm(1000*5), nrow = 1000, ncol = 5)
mcd = cwMCD(X)
heatmap_mcd(mcd, methods = c("MCD","Fisher","SSz"), top_n = 25)
}

}
