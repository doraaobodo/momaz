% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_simulations.R
\name{sim.series}
\alias{sim.series}
\title{Simulation study driver across parameter grids}
\usage{
sim.series(
  r = 100,
  m = 1000,
  d = c(5, 10),
  perout = c(0.01, 0.05),
  farout = c(0.25, 0.5),
  outlierType = c("casewise", "cellwiseStructured", "both"),
  corrType = c("independent", "ALYZ", "A09"),
  u = 0:10000/10000,
  seed = NULL
)
}
\arguments{
\item{r}{Integer. Number of simulation replicates per setting (default \code{100}).}

\item{m}{Integer. Number of markers (rows / observations) per replicate (default \code{1000}).}

\item{d}{Integer vector. Dimensions to evaluate (default \code{c(5, 10)}).}

\item{perout}{Numeric vector in [0,1]. Proportion of outliers (default \code{c(0.01, 0.05)}).}

\item{farout}{Numeric vector. Magnitude/severity of outliers (default \code{c(0.25, 0.50)}).}

\item{outlierType}{Character vector of outlier mechanisms:
\code{"casewise"}, \code{"cellwiseStructured"}, or \code{"both"} (default all three).}

\item{corrType}{Character vector of correlation structures to use when generating
the covariance matrix via \code{\link[cellWise]{generateCorMat}}
(default \code{c("independent","ALYZ","A09")}).}

\item{u}{Numeric vector in [0,1] for the EDF grid used in ROC construction (default \code{0:10000/10000}).}

\item{seed}{Optional integer. Random seed passed to \code{set.seed} (default \code{NULL}).}
}
\value{
A list with:
\describe{
\item{res.mtx}{\code{data.frame} with one row per parameter setting and columns for
the setting plus AUCs returned by \code{\link[=sim.metaZ]{sim.metaZ()}}.}
\item{res.list}{List of length equal to the number of settings; each element contains
the covariance matrix \code{S} used and the averaged ROC matrix \code{roc.mtx}.}
}
}
\description{
Runs a series of Monte Carlo simulations across all combinations of user-specified
parameters, calling \code{\link[=sim.metaZ]{sim.metaZ()}} for each setting, and aggregates AUCs and ROC curves.
}
\details{
For each row of the parameter grid (formed by \code{d}, \code{perout}, \code{farout},
\code{outlierType}, and \code{corrType}), the function calls \code{\link[=sim.metaZ]{sim.metaZ()}}, collects
the AUCs and ROC matrices, and returns both the per-setting summary table and the
list of full per-setting results.
}
\examples{
\dontrun{
set.seed(1)
out = sim.series(r = 2, m = 200, d = c(5, 10), perout = 0.05, farout = 0.5,
                  outlierType = "casewise", corrType = c("independent", "ALYZ"))
str(out$res.mtx)
}

}
