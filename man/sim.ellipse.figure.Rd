% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_cwMCD.R
\name{sim.ellipse.figure}
\alias{sim.ellipse.figure}
\title{Draw comparison of elliptical contours for outlier methods}
\usage{
sim.ellipse.figure(alpha = 0.05, rho = 0.5, n = 1000, ...)
}
\arguments{
\item{alpha}{Significance level for contours (default: 0.05).}

\item{rho}{Correlation parameter for the bivariate normal used in both the
simulation and the theoretical cellMCD ellipse (default 0.5).}

\item{n}{Number of simulated points to generate from a bivariate normal with
correlation \code{rho} (default \code{100}). Set to \code{0} to suppress
simulation.}

\item{...}{Additional arguments passed to \code{plot()}}
}
\value{
A numeric matrix with two columns (\code{x1}, \code{x2}) containing
the simulated data points if \code{n > 0}.
}
\description{
Plots the theoretical rejection contours from three outlier-detection methods
(Fisher’s method, sum-of-squared Z statistics, and cellMCD) in the
\eqn{z_1}-\eqn{z_2} plane, optionally overlaying simulated bivariate normal
data points with correlation \code{rho}.
}
\examples{
\dontrun{
# Default with n=100 simulated points
sim.ellipse.figure()


# Increase correlation
sim.ellipse.figure(rho = 0.8, n = 200)
}

}
